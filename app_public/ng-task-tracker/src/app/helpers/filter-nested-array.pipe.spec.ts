import { FilterNestedArrayPipe } from './filter-nested-array.pipe';

describe('FilterNestedArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNestedArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
